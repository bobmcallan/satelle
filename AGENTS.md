# Agent instructions (this repo)

This repo is governed by satelle. Agent behaviour is not defined here — it
lives in the satelle principles injected into every session and dispatched
agent (see `satelle-agent-goals`) and in the active workflow's skills and
gates under `.satelle/`.

For process detail, run `satelle help` and `satelle help <topic>` rather than
looking here.
