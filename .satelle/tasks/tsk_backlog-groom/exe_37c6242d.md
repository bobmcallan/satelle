---
id: exe_37c6242d
type: execution
status: done
category: chore
parent: tsk_backlog-groom
created: 2026-07-14T10:41:24Z
updated: 2026-07-14T10:48:46Z
---

# Groom epic:surface-scoped-steps

ACTION: Apply read-only epic review findings F1-F8 to epic:surface-scoped-steps.
STATUS: ACTION COMPLETE. Evidence below.

## Mutations applied (story id + before/after)

F1 — epic-parent + parent membership. APPLIED.
  CREATED sty_bbcf3999 `category: epic-parent`, tags epic:surface-scoped-steps + area:workflow, with MOTIVATION/GOAL/NON-GOALS scoped to surface-scoped steps only. Verified it stamps workflow:satelle-parent-workflow (category-specific applies_to ["epic-parent","parent"] beats the wildcard project workflow — as the principle requires).
  PARENTED: sty_034d843c, sty_c6d093c8, sty_8225d8a5, sty_dcce86d5, sty_e4359efe -> parent=sty_bbcf3999. Before: all orphans tagged epic:surface-scoped-steps only. After: durable parent links; --parent filtering now returns the set.

F2 — detach park story. APPLIED.
  sty_f75286dc: BEFORE tags epic:surface-scoped-steps, order:3. AFTER epic:park-from-any-step, order:1. Objective, body and ACs UNTOUCHED (verified: only tag mutations issued). Confirmed absent from `story list --tag epic:surface-scoped-steps` (0 hits) and present under its own theme.

F3 — first consumer. APPLIED.
  CREATED sty_e4359efe (order:5) — wire a real surface-scoped design-system gate into satelle-project-workflow, incl. dogfood AC ("gate has RUN against a real UI story"). Body records the Claude Design seed material AND its four-way layout contradiction + retired-accent-dot regression risk, so the rubric is reconciled against this repo's app.css as authority, not the kit.

F4 — altitude. RESOLVED BY OPERATOR; both parts applied.
  (a) sty_c6d093c8: retitled "...gate a scoped reviewer node on a story's surface: tags". ACs rewritten 8 -> 11 for ACCURACY (not intent): now explicitly node-level gate filtering, matching in scopedReviewers so tags decide ENQUEUEMENT; edge gates explicitly unconditional (AC5); performing nodes explicitly out of scope (AC6). Added an explicit SCOPE section (IN/OUT) to the body. No objective change — the ACs now say what the story always meant.
  (b) CREATED sty_8225d8a5 (order:3) — surface-scoped performing step via ADDITIVE RUBRIC COMPOSITION. Carries the node-vs-edge multi-skill constraint (State.Skill is a single string, wfdot.go:426-428/:92; edges splitCSVSkills to []string, :401-403), the rejected node-override alternative and WHY (ui+cli tie-break; reachability), the open on= vs augments= question left for PLAN, and the blast radius incl. the web-diagram phantom-node trap (workflow.go:88-95 splits on agent=='reviewer' && !incident — an edge-less EXECUTOR node matches neither arm).

F5 — split vocabularies. APPLIED.
  sty_034d843c: BEFORE "Controlled vocabularies: surface: tag namespace in satelle.toml, category as type" (both vocabularies). AFTER "surface: controlled tag namespace in satelle.toml" — surface ONLY, ACs 8 -> 9, stays order:1 in the epic. Removed area:config tag.
  CREATED sty_dde5aa70 STANDALONE (epic:classification-hygiene, order:1, NOT parented into this epic) for the category/type vocabulary. Preserves the operator's explicit ask (embedded substrate default + satelle.toml override, neither hardcoded in Go), carries the full drift evidence and the containsStr case-sensitivity trap, and adds the migration-phasing concern (warn before hard-reject; never rewrite terminal stories).

F6 — gameability follow-up. APPLIED.
  CREATED sty_dcce86d5 (order:4, category: architecture) — a DECISION story with three named options (deterministic path->surface rubric per the satelle-substrate-only-check precedent / advisory create-review only / explicit accepted risk). ACs require the decision be RECORDED outside a story body, and that a skipped gate leave a visible artifact rather than being indistinguishable from "no such surface".

F7 — dead id. APPLIED.
  sty_c6d093c8 body: BEFORE "Depends on sty-surface-vocabulary" (not a real id). AFTER "DEPENDS ON: sty_034d843c". Verified: dead id absent, real id present.

F8 — fail-closed blast radius. APPLIED.
  sty_c6d093c8 AC9 now requires auditing every workflow under .satelle/workflows AND internal/config/substrate/workflows for unknown attributes BEFORE enabling the reject, so nothing starts failing validate/engage on upgrade.

## Final structure (verified by `satelle story list --tag epic:surface-scoped-steps`)

  PARENT  sty_bbcf3999  epic-parent   EPIC: surface-scoped steps
  order:1 sty_034d843c  feature       surface: controlled tag namespace in satelle.toml
  order:2 sty_c6d093c8  feature       Step-level applies_to: gate a scoped reviewer node
  order:3 sty_8225d8a5  feature       Surface-scoped performing step (additive composition)
  order:4 sty_dcce86d5  architecture  Decide: is a self-declared surface: tag trustworthy?
  order:5 sty_e4359efe  feature       First consumer: design-system gate + dogfood

  Contiguous order 1-5, no duplicates, every member parented. Outside the epic:
  sty_f75286dc (epic:park-from-any-step, order:1) and sty_dde5aa70 (epic:classification-hygiene, order:1).

## Escalations

NONE. No finding required an objective change. F4 was the only ambiguity and the operator resolved it before this execution was created (decision: reviewer nodes AND a surface-scoped performing step), so it was applied as AC accuracy (a) plus a new story (b) rather than escalated.

## Notes for the operator (not blocking, not applied)

1. SUBSTRATE FRICTION HIT DURING THIS RUN: `satelle execution create --category execution` is REJECTED by satelle-story-create-review ("category `execution` is the task-run workflow key, not a story leaf class"), while omitting --category is also rejected ("category is empty — pass --category"). The reviewer's suggested fix ("create as `satelle execution create --parent tsk_backlog-groom`") was ALREADY the command being run. Worked around with --category chore. An execution's KIND already resolves the task workflow, so requiring a leaf category on an execution — and rejecting the one that matches its kind — is a trap worth a story.
2. order: was left on sty_f75286dc and sty_dde5aa70 as order:1 within their OWN themes; each is currently a single-member theme.

## Acceptance Criteria

1. Every applied mutation is listed with story id and before/after summary.
2. No member story OBJECTIVE or AC intent altered — only HOW (order, tags, split, accuracy).
3. Escalations explicit; nothing silently guessed.
4. epic:surface-scoped-steps has an epic-parent; members link via parent; order:N contiguous from 1.
5. sty_f75286dc detached (no epic:surface-scoped-steps, no order:3), objective untouched.
