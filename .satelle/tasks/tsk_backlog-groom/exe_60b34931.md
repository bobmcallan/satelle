---
id: exe_60b34931
type: execution
status: done
category: process
parent: tsk_backlog-groom
created: 2026-07-15T09:39:20Z
updated: 2026-07-15T09:40:54Z
---

# Groom epic:workspace-rehydrate

## Findings
From read-only review of epic:workspace-rehydrate (sty_4ae354f4):

1. order:3 conflict policy required but not pinned in AC
2. order:3 server workstate list/get is blocking and untracked
3. order:3 materialize should be store-first + view regen (not file-only)
4. order:4 empty-tree bootstrap sequence not pinned (bind then config deploy then pulls)
5. order:4 bare-sync reorientation is MAY; rehydrate entrypoint is MUST
6. Epic parent still backlog with children done → in_progress
7. order:3/4 missing estimate tags (hygiene)

## Apply-to
- sty_4ae354f4, sty_45bfcc50, sty_2f1538a4
- allowed: reorder/retag/split/AC-body accuracy
- FORBIDDEN: change any story OBJECTIVE or AC intent

## Escalation
- sty_4ae354f4 status backlog→in_progress rejected: transition not a declared edge on satelle-parent-workflow. Left backlog; not forced. Operator may engage the epic through its normal parent path if desired.
- Deferred non-goals (whole-dir gitignore flip, home-keyed DB) left unfiled (optional stubs; not required by findings).

## Mutations applied

### sty_45bfcc50 (order:3) — body + AC accuracy + estimates
- BEFORE: conflict policy only "document and implement"; materialize vague on files/DB; server gap mentioned but not AC-gated; no estimates.
- AFTER:
  - Body: MATERIALIZE store-first + view regen; CONFLICT POLICY pinned (empty→hosted; both non-empty→fail + optional force); SERVER DEPENDENCY blocking HOW (no client-only dead-end).
  - AC2: store upsert + derived view regen (intent unchanged: recover after wipe).
  - AC4: pinned conflict default (intent unchanged: no silent clobber).
  - AC6 NEW accuracy: server list/get callable or server story filed and done deferred (makes existing "file server follow-up" enforceable).
  - Tags added: estimate-minutes:120, estimate-tokens:90000
- OBJECTIVE unchanged: workstate pull from personal hosted.

### sty_2f1538a4 (order:4) — body + AC accuracy + estimates
- BEFORE: sequence and must/may for bare sync unspecified; AC1 path lacked config-deploy-first.
- AFTER:
  - Body: MUST vs MAY; PINNED EMPTY-TREE SEQUENCE (install→login→bind→config deploy→docs+workstate pull); note scopes live in satelle.toml.
  - AC1: includes config deploy first (HOW of existing recover path).
  - AC2: marked MUST.
  - AC3: bind then materialize with config deploy before scope-gated pulls.
  - AC6 NEW: bare sync reorientation MAY; docs enough if not reoriented.
  - Tags added: estimate-minutes:150, estimate-tokens:110000
- OBJECTIVE unchanged: sync-down rehydrate on empty tree.

### sty_4ae354f4 (epic parent) — attempted status only
- NOT applied (workflow edge missing). No body/AC change.

### sty_c5c54f02 / sty_1eaa15f5
- Unchanged (already done; AC2 of epic already met).

## Verification
1. Enumeration: satelle story list --tag epic:workspace-rehydrate
2. Objectives of order:3 and order:4 unchanged (pull workstate; rehydrate empty tree).
3. All applied mutations are HOW/accuracy only; AC intent preserved or made testable without expanding product goal.
4. Escalation on epic status recorded; no silent force.

## Acceptance Criteria

1. All injected findings applied as HOW-only mutations (or escalated explicitly).
2. Mutation list with story id and before/after is recorded on the execution.
3. No story OBJECTIVE or AC intent changed.
4. epic:workspace-rehydrate members re-list cleanly with updated order/status/AC accuracy.
